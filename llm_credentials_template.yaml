#
#  No real credentials must be stored in this file. This is a template
#  Copy the file to ~/.llm/credentials.yaml and change the mode to 400
#

openai:
  default:
    api_key: sk-xxxx
  research:
    api_key: sk-yyyy

huggingface:
  default:
    token: hf-aaa
  fallback:
    token: hf-bbb

azure:
  default:
    api_key: az-xxx
  prod:
    api_key: az-yyy
